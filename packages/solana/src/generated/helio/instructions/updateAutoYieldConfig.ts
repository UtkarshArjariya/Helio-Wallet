/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";
import {
  getAutoYieldConfigArgsDecoder,
  getAutoYieldConfigArgsEncoder,
  type AutoYieldConfigArgs,
  type AutoYieldConfigArgsArgs,
} from "../types";

export const UPDATE_AUTO_YIELD_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([36, 48, 204, 198, 62, 156, 39, 92]);

export function getUpdateAutoYieldConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_AUTO_YIELD_CONFIG_DISCRIMINATOR,
  );
}

export type UpdateAutoYieldConfigInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAutoYieldConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  args: AutoYieldConfigArgs;
};

export type UpdateAutoYieldConfigInstructionDataArgs = {
  args: AutoYieldConfigArgsArgs;
};

export function getUpdateAutoYieldConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateAutoYieldConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["args", getAutoYieldConfigArgsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_AUTO_YIELD_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getUpdateAutoYieldConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateAutoYieldConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["args", getAutoYieldConfigArgsDecoder()],
  ]);
}

export function getUpdateAutoYieldConfigInstructionDataCodec(): FixedSizeCodec<
  UpdateAutoYieldConfigInstructionDataArgs,
  UpdateAutoYieldConfigInstructionData
> {
  return combineCodec(
    getUpdateAutoYieldConfigInstructionDataEncoder(),
    getUpdateAutoYieldConfigInstructionDataDecoder(),
  );
}

export type UpdateAutoYieldConfigAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
  args: UpdateAutoYieldConfigInstructionDataArgs["args"];
};

export async function getUpdateAutoYieldConfigInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: UpdateAutoYieldConfigAsyncInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateAutoYieldConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getUpdateAutoYieldConfigInstructionDataEncoder().encode(
      args as UpdateAutoYieldConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAutoYieldConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type UpdateAutoYieldConfigInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
  args: UpdateAutoYieldConfigInstructionDataArgs["args"];
};

export function getUpdateAutoYieldConfigInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: UpdateAutoYieldConfigInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): UpdateAutoYieldConfigInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getUpdateAutoYieldConfigInstructionDataEncoder().encode(
      args as UpdateAutoYieldConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAutoYieldConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type ParsedUpdateAutoYieldConfigInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: UpdateAutoYieldConfigInstructionData;
};

export function parseUpdateAutoYieldConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAutoYieldConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), config: getNextAccount() },
    data: getUpdateAutoYieldConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
