/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findReserveStatePda, findSolVaultPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";

export const SWEEP_SOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  48, 81, 27, 227, 28, 145, 224, 204,
]);

export function getSweepSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SWEEP_SOL_DISCRIMINATOR);
}

export type SweepSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountReserveState extends string | AccountMeta<string> = string,
  TAccountSolVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountReserveState extends string
        ? WritableAccount<TAccountReserveState>
        : TAccountReserveState,
      TAccountSolVault extends string
        ? WritableAccount<TAccountSolVault>
        : TAccountSolVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SweepSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountLamports: bigint;
};

export type SweepSolInstructionDataArgs = { amountLamports: number | bigint };

export function getSweepSolInstructionDataEncoder(): FixedSizeEncoder<SweepSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountLamports", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SWEEP_SOL_DISCRIMINATOR }),
  );
}

export function getSweepSolInstructionDataDecoder(): FixedSizeDecoder<SweepSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountLamports", getU64Decoder()],
  ]);
}

export function getSweepSolInstructionDataCodec(): FixedSizeCodec<
  SweepSolInstructionDataArgs,
  SweepSolInstructionData
> {
  return combineCodec(
    getSweepSolInstructionDataEncoder(),
    getSweepSolInstructionDataDecoder(),
  );
}

export type SweepSolAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountReserveState extends string = string,
  TAccountSolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
  reserveState?: Address<TAccountReserveState>;
  solVault?: Address<TAccountSolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountLamports: SweepSolInstructionDataArgs["amountLamports"];
};

export async function getSweepSolInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountReserveState extends string,
  TAccountSolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: SweepSolAsyncInput<
    TAccountOwner,
    TAccountConfig,
    TAccountReserveState,
    TAccountSolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SweepSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountReserveState,
    TAccountSolVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    reserveState: { value: input.reserveState ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.reserveState.value) {
    accounts.reserveState.value = await findReserveStatePda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.solVault.value) {
    accounts.solVault.value = await findSolVaultPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
      getAccountMeta("reserveState", accounts.reserveState),
      getAccountMeta("solVault", accounts.solVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSweepSolInstructionDataEncoder().encode(
      args as SweepSolInstructionDataArgs,
    ),
    programAddress,
  } as SweepSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountReserveState,
    TAccountSolVault,
    TAccountSystemProgram
  >);
}

export type SweepSolInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountReserveState extends string = string,
  TAccountSolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
  reserveState: Address<TAccountReserveState>;
  solVault: Address<TAccountSolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountLamports: SweepSolInstructionDataArgs["amountLamports"];
};

export function getSweepSolInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountReserveState extends string,
  TAccountSolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: SweepSolInput<
    TAccountOwner,
    TAccountConfig,
    TAccountReserveState,
    TAccountSolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SweepSolInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountConfig,
  TAccountReserveState,
  TAccountSolVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    reserveState: { value: input.reserveState ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
      getAccountMeta("reserveState", accounts.reserveState),
      getAccountMeta("solVault", accounts.solVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSweepSolInstructionDataEncoder().encode(
      args as SweepSolInstructionDataArgs,
    ),
    programAddress,
  } as SweepSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountReserveState,
    TAccountSolVault,
    TAccountSystemProgram
  >);
}

export type ParsedSweepSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
    reserveState: TAccountMetas[2];
    solVault: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SweepSolInstructionData;
};

export function parseSweepSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSweepSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      config: getNextAccount(),
      reserveState: getNextAccount(),
      solVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSweepSolInstructionDataDecoder().decode(instruction.data),
  };
}
