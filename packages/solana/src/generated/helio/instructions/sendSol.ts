/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findSolVaultPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";

export const SEND_SOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  214, 24, 219, 18, 3, 205, 201, 179,
]);

export function getSendSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEND_SOL_DISCRIMINATOR);
}

export type SendSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountSolVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountSolVault extends string
        ? WritableAccount<TAccountSolVault>
        : TAccountSolVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SendSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountLamports: bigint;
  sweepBps: number;
};

export type SendSolInstructionDataArgs = {
  amountLamports: number | bigint;
  sweepBps: number;
};

export function getSendSolInstructionDataEncoder(): FixedSizeEncoder<SendSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountLamports", getU64Encoder()],
      ["sweepBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SEND_SOL_DISCRIMINATOR }),
  );
}

export function getSendSolInstructionDataDecoder(): FixedSizeDecoder<SendSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountLamports", getU64Decoder()],
    ["sweepBps", getU16Decoder()],
  ]);
}

export function getSendSolInstructionDataCodec(): FixedSizeCodec<
  SendSolInstructionDataArgs,
  SendSolInstructionData
> {
  return combineCodec(
    getSendSolInstructionDataEncoder(),
    getSendSolInstructionDataDecoder(),
  );
}

export type SendSolAsyncInput<
  TAccountOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountSolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  recipient: Address<TAccountRecipient>;
  solVault?: Address<TAccountSolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountLamports: SendSolInstructionDataArgs["amountLamports"];
  sweepBps: SendSolInstructionDataArgs["sweepBps"];
};

export async function getSendSolInstructionAsync<
  TAccountOwner extends string,
  TAccountRecipient extends string,
  TAccountSolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: SendSolAsyncInput<
    TAccountOwner,
    TAccountRecipient,
    TAccountSolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SendSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountRecipient,
    TAccountSolVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.solVault.value) {
    accounts.solVault.value = await findSolVaultPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("solVault", accounts.solVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSendSolInstructionDataEncoder().encode(
      args as SendSolInstructionDataArgs,
    ),
    programAddress,
  } as SendSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountRecipient,
    TAccountSolVault,
    TAccountSystemProgram
  >);
}

export type SendSolInput<
  TAccountOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountSolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  recipient: Address<TAccountRecipient>;
  solVault: Address<TAccountSolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountLamports: SendSolInstructionDataArgs["amountLamports"];
  sweepBps: SendSolInstructionDataArgs["sweepBps"];
};

export function getSendSolInstruction<
  TAccountOwner extends string,
  TAccountRecipient extends string,
  TAccountSolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: SendSolInput<
    TAccountOwner,
    TAccountRecipient,
    TAccountSolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SendSolInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountRecipient,
  TAccountSolVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("solVault", accounts.solVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSendSolInstructionDataEncoder().encode(
      args as SendSolInstructionDataArgs,
    ),
    programAddress,
  } as SendSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountRecipient,
    TAccountSolVault,
    TAccountSystemProgram
  >);
}

export type ParsedSendSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    recipient: TAccountMetas[1];
    solVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SendSolInstructionData;
};

export function parseSendSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSendSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      recipient: getNextAccount(),
      solVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSendSolInstructionDataDecoder().decode(instruction.data),
  };
}
