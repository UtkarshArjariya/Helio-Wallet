/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";

export const RESUME_AUTO_YIELD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([214, 119, 163, 153, 185, 215, 243, 65]);

export function getResumeAutoYieldDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESUME_AUTO_YIELD_DISCRIMINATOR,
  );
}

export type ResumeAutoYieldInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ResumeAutoYieldInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResumeAutoYieldInstructionDataArgs = {};

export function getResumeAutoYieldInstructionDataEncoder(): FixedSizeEncoder<ResumeAutoYieldInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESUME_AUTO_YIELD_DISCRIMINATOR }),
  );
}

export function getResumeAutoYieldInstructionDataDecoder(): FixedSizeDecoder<ResumeAutoYieldInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResumeAutoYieldInstructionDataCodec(): FixedSizeCodec<
  ResumeAutoYieldInstructionDataArgs,
  ResumeAutoYieldInstructionData
> {
  return combineCodec(
    getResumeAutoYieldInstructionDataEncoder(),
    getResumeAutoYieldInstructionDataDecoder(),
  );
}

export type ResumeAutoYieldAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
};

export async function getResumeAutoYieldInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: ResumeAutoYieldAsyncInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ResumeAutoYieldInstruction<TProgramAddress, TAccountOwner, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getResumeAutoYieldInstructionDataEncoder().encode({}),
    programAddress,
  } as ResumeAutoYieldInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type ResumeAutoYieldInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
};

export function getResumeAutoYieldInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: ResumeAutoYieldInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): ResumeAutoYieldInstruction<TProgramAddress, TAccountOwner, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getResumeAutoYieldInstructionDataEncoder().encode({}),
    programAddress,
  } as ResumeAutoYieldInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type ParsedResumeAutoYieldInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: ResumeAutoYieldInstructionData;
};

export function parseResumeAutoYieldInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResumeAutoYieldInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), config: getNextAccount() },
    data: getResumeAutoYieldInstructionDataDecoder().decode(instruction.data),
  };
}
