/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_AUTO_YIELD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([211, 43, 244, 12, 113, 41, 221, 214]);

export function getPauseAutoYieldDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAUSE_AUTO_YIELD_DISCRIMINATOR,
  );
}

export type PauseAutoYieldInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type PauseAutoYieldInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PauseAutoYieldInstructionDataArgs = {};

export function getPauseAutoYieldInstructionDataEncoder(): FixedSizeEncoder<PauseAutoYieldInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAUSE_AUTO_YIELD_DISCRIMINATOR }),
  );
}

export function getPauseAutoYieldInstructionDataDecoder(): FixedSizeDecoder<PauseAutoYieldInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPauseAutoYieldInstructionDataCodec(): FixedSizeCodec<
  PauseAutoYieldInstructionDataArgs,
  PauseAutoYieldInstructionData
> {
  return combineCodec(
    getPauseAutoYieldInstructionDataEncoder(),
    getPauseAutoYieldInstructionDataDecoder(),
  );
}

export type PauseAutoYieldAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
};

export async function getPauseAutoYieldInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: PauseAutoYieldAsyncInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PauseAutoYieldInstruction<TProgramAddress, TAccountOwner, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getPauseAutoYieldInstructionDataEncoder().encode({}),
    programAddress,
  } as PauseAutoYieldInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type PauseAutoYieldInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
};

export function getPauseAutoYieldInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: PauseAutoYieldInput<TAccountOwner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): PauseAutoYieldInstruction<TProgramAddress, TAccountOwner, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("config", accounts.config),
    ],
    data: getPauseAutoYieldInstructionDataEncoder().encode({}),
    programAddress,
  } as PauseAutoYieldInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig
  >);
}

export type ParsedPauseAutoYieldInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: PauseAutoYieldInstructionData;
};

export function parsePauseAutoYieldInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPauseAutoYieldInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), config: getNextAccount() },
    data: getPauseAutoYieldInstructionDataDecoder().decode(instruction.data),
  };
}
