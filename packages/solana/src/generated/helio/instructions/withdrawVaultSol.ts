/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findSolVaultPda } from "../pdas";
import { HELIO_PROGRAM_ADDRESS } from "../programs";

export const WITHDRAW_VAULT_SOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([3, 23, 239, 50, 93, 233, 102, 85]);

export function getWithdrawVaultSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_VAULT_SOL_DISCRIMINATOR,
  );
}

export type WithdrawVaultSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSolVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSolVault extends string
        ? WritableAccount<TAccountSolVault>
        : TAccountSolVault,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawVaultSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountLamports: bigint;
};

export type WithdrawVaultSolInstructionDataArgs = {
  amountLamports: number | bigint;
};

export function getWithdrawVaultSolInstructionDataEncoder(): FixedSizeEncoder<WithdrawVaultSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountLamports", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_VAULT_SOL_DISCRIMINATOR }),
  );
}

export function getWithdrawVaultSolInstructionDataDecoder(): FixedSizeDecoder<WithdrawVaultSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountLamports", getU64Decoder()],
  ]);
}

export function getWithdrawVaultSolInstructionDataCodec(): FixedSizeCodec<
  WithdrawVaultSolInstructionDataArgs,
  WithdrawVaultSolInstructionData
> {
  return combineCodec(
    getWithdrawVaultSolInstructionDataEncoder(),
    getWithdrawVaultSolInstructionDataDecoder(),
  );
}

export type WithdrawVaultSolAsyncInput<
  TAccountOwner extends string = string,
  TAccountSolVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  solVault?: Address<TAccountSolVault>;
  amountLamports: WithdrawVaultSolInstructionDataArgs["amountLamports"];
};

export async function getWithdrawVaultSolInstructionAsync<
  TAccountOwner extends string,
  TAccountSolVault extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: WithdrawVaultSolAsyncInput<TAccountOwner, TAccountSolVault>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawVaultSolInstruction<TProgramAddress, TAccountOwner, TAccountSolVault>
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.solVault.value) {
    accounts.solVault.value = await findSolVaultPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("solVault", accounts.solVault),
    ],
    data: getWithdrawVaultSolInstructionDataEncoder().encode(
      args as WithdrawVaultSolInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawVaultSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountSolVault
  >);
}

export type WithdrawVaultSolInput<
  TAccountOwner extends string = string,
  TAccountSolVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  solVault: Address<TAccountSolVault>;
  amountLamports: WithdrawVaultSolInstructionDataArgs["amountLamports"];
};

export function getWithdrawVaultSolInstruction<
  TAccountOwner extends string,
  TAccountSolVault extends string,
  TProgramAddress extends Address = typeof HELIO_PROGRAM_ADDRESS,
>(
  input: WithdrawVaultSolInput<TAccountOwner, TAccountSolVault>,
  config?: { programAddress?: TProgramAddress },
): WithdrawVaultSolInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountSolVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? HELIO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    solVault: { value: input.solVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("solVault", accounts.solVault),
    ],
    data: getWithdrawVaultSolInstructionDataEncoder().encode(
      args as WithdrawVaultSolInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawVaultSolInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountSolVault
  >);
}

export type ParsedWithdrawVaultSolInstruction<
  TProgram extends string = typeof HELIO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    solVault: TAccountMetas[1];
  };
  data: WithdrawVaultSolInstructionData;
};

export function parseWithdrawVaultSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawVaultSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), solVault: getNextAccount() },
    data: getWithdrawVaultSolInstructionDataDecoder().decode(instruction.data),
  };
}
